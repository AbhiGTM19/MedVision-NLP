{
  "handoff_id": "SYNC_MEDICAL_CLASSIFICATION",
  "source_role": "Integration QA Specialist",
  "target_roles": ["Lead ML/Backend Engineer", "Lead Frontend Engineer"],
  "payload": {
    "api_endpoints": {
      "predict": "/predict",
      "predict_image": "/predict-image",
      "predict_rag": "/predict-rag",
      "chat": "/chat",
      "health": "/health",
      "metrics": "/metrics",
      "monitoring": "/monitoring"
    },
    "input_schema": {
      "PredictionRequest": {
        "text": "string (required, min_length=1, max_length=5000)"
      },
      "ImageUpload": {
        "file": "UploadFile (FormData, required image file)"
      }
    },
    "output_schema": {
      "PredictionResponse": {
        "specialty": "string",
        "confidence": "float",
        "word_attributions": [
          {
            "word": "string",
            "score": "float"
          }
        ],
        "extracted_text": "string (optional)",
        "error": "string (optional)"
      }
    },
    "models": {
      "Bio_ClinicalBERT_Classifier": {
        "type": "SequenceClassification + Captum XAI",
        "model_path": "backend/models/bio_clinicalBERT/bio_clinicalBERT_model.pth",
        "hf_repo_id": "abhshkgtm19/medvision-models"
      }
    }
  },
  "required_actions": {
    "frontend": "Ensure text submissions use JSON body for /predict and FormData for /predict-image. Parse 'word_attributions' array for Captum XAI highlighting.",
    "ml": "Maintain eager singleton loading of models, ChromaDB, and GenAI clients."
  }
}
