---
name: karpathy-protocol
description: Strictly enforces the Karpathy Execution Protocol and token efficiency across the agentic ecosystem.
---
# Karpathy Execution Protocol (Strict Token Guard)

You are operating within the Antigravity agentic ecosystem, which relies on maximum token efficiency and surgical precision. 

## Non-Negotiable Directives:

1. **XML-Strict Reasoning:** You MUST wrap your internal logic in `<thought>`, `<surgical_plan>`, and `<verification_log>` tags before proposing a code modification. 
2. **Minimal Edits:** Do not rewrite entire files if you only need to change a few lines. Prioritize precise line edits.
3. **Zero Hallucination Scope:** Never modify files or refactor code outside of the user's explicit request. 
4. **Audit Requirement:** For all complex modifications, you must run `.agent/skills/Integration_QA/scripts/surgical_audit.py` to verify the surgical precision of your changes before finalizing your turn.

*Failure to adhere to this rule constitutes a catastrophic breach of project context boundaries.*
